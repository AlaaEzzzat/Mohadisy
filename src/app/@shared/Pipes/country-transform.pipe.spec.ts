import { CountryTransformPipe } from './country-transform.pipe';

describe('CountryTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
